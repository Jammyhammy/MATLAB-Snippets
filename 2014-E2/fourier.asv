function F = fourier( Ai, T, n )
%UNTITLED9 Summary of this function goes here
%   Detailed explanation goes here
sign = 0;
F(0) = (((4*Ai)/(2*n-1)*pi) * sin((2*pi*(2*pi-1) * t )/T));
t = linspace(0,T,T/2);
for i = 1:n
    if sign
    F(i) = (((4*Ai)/(2*n-1)*pi) * sin((2*pi*(2*pi-1) * t )/T));
end

